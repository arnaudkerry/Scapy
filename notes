Créer un paquet IP
    a = IP(dst="www.google.com")

Créer un paquet Http
    b = TCP(dport=80)

Encapsuler des paquets 
    c = a/b

Envoyer un paquet
    send(c) ou sr(c)

Montrer le contenu d'un paquet
    c.show()

Avoir la vue en octet d'un paquet
    raw(c) ou hexdump(c)

